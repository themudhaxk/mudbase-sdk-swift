//
// WalletAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class WalletAPI {

    /**
     Broadcast a client-signed transaction
     
     - parameter walletBroadcastTransactionRequest: (body) Chain, signed transaction (hex), and fromAddress (must be registered). 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletBroadcastTransaction(walletBroadcastTransactionRequest: WalletBroadcastTransactionRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletBroadcastTransaction200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletBroadcastTransactionWithRequestBuilder(walletBroadcastTransactionRequest: walletBroadcastTransactionRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Broadcast a client-signed transaction
     - POST /api/wallet/non-custodial/broadcast
     - Broadcast a transaction that has been signed client-side. The transaction must be fully signed before sending. The fromAddress must be registered and belong to your organization (POST /api/wallet/non-custodial/register-address). **Supported chains:** EVM (ethereum, polygon, arbitrum, optimism, base, bsc, binance, avalanche, celo), UTXO (bitcoin, litecoin, dogecoin), and chain-specific (tron, solana, ton, cardano). Use `binance` or `bsc` for BNB Smart Chain. **Testing with custodial:** You can create a wallet via POST /api/wallet/create, get its private key via GET /api/wallet/{walletId}/private-key, register that address with POST /api/wallet/non-custodial/register-address, then build a signed tx (using POST /api/wallet/estimate-network-fee or estimate-gas for fees) and broadcast it here to test the non-custodial flow end-to-end. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletBroadcastTransactionRequest: (body) Chain, signed transaction (hex), and fromAddress (must be registered). 
     - returns: RequestBuilder<WalletBroadcastTransaction200Response> 
     */
    open class func walletBroadcastTransactionWithRequestBuilder(walletBroadcastTransactionRequest: WalletBroadcastTransactionRequest) -> RequestBuilder<WalletBroadcastTransaction200Response> {
        let localVariablePath = "/api/wallet/non-custodial/broadcast"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletBroadcastTransactionRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletBroadcastTransaction200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Create a wallet webhook
     
     - parameter walletCreateWebhookRequest: (body) Webhook URL, events array, optional secret and filters (addresses, chains, projectId). 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletCreateWebhook(walletCreateWebhookRequest: WalletCreateWebhookRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletCreateWebhook201Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletCreateWebhookWithRequestBuilder(walletCreateWebhookRequest: walletCreateWebhookRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Create a wallet webhook
     - POST /api/wallet/non-custodial/webhooks
     - Register a webhook URL to receive wallet events (balance updates, transaction confirmed/failed/detected/broadcast, token balance, address created/deactivated). Optional filters by addresses and chains. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletCreateWebhookRequest: (body) Webhook URL, events array, optional secret and filters (addresses, chains, projectId). 
     - returns: RequestBuilder<WalletCreateWebhook201Response> 
     */
    open class func walletCreateWebhookWithRequestBuilder(walletCreateWebhookRequest: WalletCreateWebhookRequest) -> RequestBuilder<WalletCreateWebhook201Response> {
        let localVariablePath = "/api/wallet/non-custodial/webhooks"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletCreateWebhookRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletCreateWebhook201Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Delete (soft-delete) a non-custodial address
     
     - parameter addressId: (path) Registered address ID to delete. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletDeleteAddress(addressId: String, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: FunctionsDelete200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletDeleteAddressWithRequestBuilder(addressId: addressId).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Delete (soft-delete) a non-custodial address
     - DELETE /api/wallet/non-custodial/addresses/{addressId}
     - Soft-deletes a registered non-custodial address. The address is marked inactive and no longer used for broadcasts or balance/transaction queries. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter addressId: (path) Registered address ID to delete. 
     - returns: RequestBuilder<FunctionsDelete200Response> 
     */
    open class func walletDeleteAddressWithRequestBuilder(addressId: String) -> RequestBuilder<FunctionsDelete200Response> {
        var localVariablePath = "/api/wallet/non-custodial/addresses/{addressId}"
        let addressIdPreEscape = "\(APIHelper.mapValueToPathItem(addressId))"
        let addressIdPostEscape = addressIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{addressId}", with: addressIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<FunctionsDelete200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Delete a wallet webhook
     
     - parameter webhookId: (path) Webhook ID (MongoDB ObjectId) to delete. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletDeleteWebhook(webhookId: String, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: FunctionsDelete200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletDeleteWebhookWithRequestBuilder(webhookId: webhookId).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Delete a wallet webhook
     - DELETE /api/wallet/non-custodial/webhooks/{webhookId}
     - Permanently delete a wallet webhook. Delivery will stop immediately.
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter webhookId: (path) Webhook ID (MongoDB ObjectId) to delete. 
     - returns: RequestBuilder<FunctionsDelete200Response> 
     */
    open class func walletDeleteWebhookWithRequestBuilder(webhookId: String) -> RequestBuilder<FunctionsDelete200Response> {
        var localVariablePath = "/api/wallet/non-custodial/webhooks/{webhookId}"
        let webhookIdPreEscape = "\(APIHelper.mapValueToPathItem(webhookId))"
        let webhookIdPostEscape = webhookIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{webhookId}", with: webhookIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<FunctionsDelete200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Estimate network fee from blockchain (all supported chains; not controlled by Mudbase)
     
     - parameter walletEstimateGasRequest: (body) Chain and optional transaction shape (required for EVM). Returns network fee from blockchain. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletEstimateGas(walletEstimateGasRequest: WalletEstimateGasRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletEstimateGas200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletEstimateGasWithRequestBuilder(walletEstimateGasRequest: walletEstimateGasRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Estimate network fee from blockchain (all supported chains; not controlled by Mudbase)
     - POST /api/wallet/non-custodial/estimate-gas
     - **Network fee (from blockchain only).** Returns network fee **estimated directly from the blockchain** via RPC or fee APIs. **Not controlled by Mudbase.** Both POST /api/wallet/estimate-network-fee (or calculate-fee) and this endpoint return network fee only; use either for gas/fee display. This endpoint is chain-oriented and supports full transaction shape for EVM. **EVM chains:** ethereum, polygon, arbitrum, optimism, base, bsc, binance, avalanche, celo — require `transaction` (from, and to/value or tokenAddress/amount). Response includes gasLimit, gasPrice, networkFee, estimatedTime, currency. **Non-EVM chains:** bitcoin, litecoin, dogecoin, solana, tron, ton, cardano — only `chain` is required; `transaction` is optional/ignored. Returns networkFee, estimatedTime, currency (and e.g. satPerVb for UTXO). See docs/FEE_ARCHITECTURE.md. Results cached 15s. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletEstimateGasRequest: (body) Chain and optional transaction shape (required for EVM). Returns network fee from blockchain. 
     - returns: RequestBuilder<WalletEstimateGas200Response> 
     */
    open class func walletEstimateGasWithRequestBuilder(walletEstimateGasRequest: WalletEstimateGasRequest) -> RequestBuilder<WalletEstimateGas200Response> {
        let localVariablePath = "/api/wallet/non-custodial/estimate-gas"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletEstimateGasRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletEstimateGas200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get non-custodial address by ID
     
     - parameter addressId: (path) Registered address ID (MongoDB ObjectId). 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetAddress(addressId: String, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: NonCustodialAddressResponse?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetAddressWithRequestBuilder(addressId: addressId).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get non-custodial address by ID
     - GET /api/wallet/non-custodial/addresses/{addressId}
     - Returns metadata and status for a single registered non-custodial address (ID, address, chain, label, org, project, derivationPath, isActive, timestamps). 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter addressId: (path) Registered address ID (MongoDB ObjectId). 
     - returns: RequestBuilder<NonCustodialAddressResponse> 
     */
    open class func walletGetAddressWithRequestBuilder(addressId: String) -> RequestBuilder<NonCustodialAddressResponse> {
        var localVariablePath = "/api/wallet/non-custodial/addresses/{addressId}"
        let addressIdPreEscape = "\(APIHelper.mapValueToPathItem(addressId))"
        let addressIdPostEscape = addressIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{addressId}", with: addressIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<NonCustodialAddressResponse>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get balance for a non-custodial address
     
     - parameter addressId: (path) Registered address ID. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetBalance(addressId: String, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletGetBalance200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetBalanceWithRequestBuilder(addressId: addressId).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get balance for a non-custodial address
     - GET /api/wallet/non-custodial/addresses/{addressId}/balance
     - Returns native token balance (confirmed, unconfirmed, total, currency) for a registered non-custodial address. Updated periodically from the chain. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter addressId: (path) Registered address ID. 
     - returns: RequestBuilder<WalletGetBalance200Response> 
     */
    open class func walletGetBalanceWithRequestBuilder(addressId: String) -> RequestBuilder<WalletGetBalance200Response> {
        var localVariablePath = "/api/wallet/non-custodial/addresses/{addressId}/balance"
        let addressIdPreEscape = "\(APIHelper.mapValueToPathItem(addressId))"
        let addressIdPostEscape = addressIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{addressId}", with: addressIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletGetBalance200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get replacement tx params for cancel (stuck EVM tx)
     
     - parameter walletGetCancelParamsRequest: (body) Stuck transaction identifier (txId or txHash) and EVM chain for cancel. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetCancelParams(walletGetCancelParamsRequest: WalletGetCancelParamsRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletGetCancelParams200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetCancelParamsWithRequestBuilder(walletGetCancelParamsRequest: walletGetCancelParamsRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get replacement tx params for cancel (stuck EVM tx)
     - POST /api/wallet/non-custodial/cancel
     - Returns **replacement transaction params** to cancel a stuck EVM transaction (same nonce, to=self, value=0, data=0x, higher gas). Client signs and broadcasts via POST /api/wallet/non-custodial/broadcast. Address must be registered for your organization. EVM chains only. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletGetCancelParamsRequest: (body) Stuck transaction identifier (txId or txHash) and EVM chain for cancel. 
     - returns: RequestBuilder<WalletGetCancelParams200Response> 
     */
    open class func walletGetCancelParamsWithRequestBuilder(walletGetCancelParamsRequest: WalletGetCancelParamsRequest) -> RequestBuilder<WalletGetCancelParams200Response> {
        let localVariablePath = "/api/wallet/non-custodial/cancel"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletGetCancelParamsRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletGetCancelParams200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get replacement tx params for speed-up (stuck EVM tx)
     
     - parameter walletGetSpeedUpParamsRequest: (body) Stuck transaction identifier (txId or txHash) and EVM chain. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetSpeedUpParams(walletGetSpeedUpParamsRequest: WalletGetSpeedUpParamsRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletGetSpeedUpParams200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetSpeedUpParamsWithRequestBuilder(walletGetSpeedUpParamsRequest: walletGetSpeedUpParamsRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get replacement tx params for speed-up (stuck EVM tx)
     - POST /api/wallet/non-custodial/speed-up
     - Returns **replacement transaction params** for a stuck EVM transaction (same nonce, same to/value/data, higher gas). Client signs the replacement and broadcasts via POST /api/wallet/non-custodial/broadcast. Address must be registered for your organization. Use when a tx has been pending >5 min (stuck). EVM chains only. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletGetSpeedUpParamsRequest: (body) Stuck transaction identifier (txId or txHash) and EVM chain. 
     - returns: RequestBuilder<WalletGetSpeedUpParams200Response> 
     */
    open class func walletGetSpeedUpParamsWithRequestBuilder(walletGetSpeedUpParamsRequest: WalletGetSpeedUpParamsRequest) -> RequestBuilder<WalletGetSpeedUpParams200Response> {
        let localVariablePath = "/api/wallet/non-custodial/speed-up"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletGetSpeedUpParamsRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletGetSpeedUpParams200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     * enum for parameter chain
     */
    public enum Chain_walletGetTransactionByHash: String, CaseIterable {
        case ethereum = "ethereum"
        case binance = "binance"
        case polygon = "polygon"
        case celo = "celo"
        case bitcoin = "bitcoin"
        case litecoin = "litecoin"
        case solana = "solana"
        case tron = "tron"
        case ripple = "ripple"
        case cardano = "cardano"
        case dogecoin = "dogecoin"
        case ton = "ton"
    }

    /**
     Get transaction by hash
     
     - parameter txHash: (path) Transaction hash (e.g. 0x... for EVM, or block explorer format for UTXO) 
     - parameter chain: (query) Chain the transaction belongs to (required for lookup) 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetTransactionByHash(txHash: String, chain: Chain_walletGetTransactionByHash, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletGetTransactionByHash200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetTransactionByHashWithRequestBuilder(txHash: txHash, chain: chain).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get transaction by hash
     - GET /api/wallet/non-custodial/transactions/{txHash}
     - Returns a transaction by its hash. The **chain** query parameter is required because the same hash format can exist on different chains (e.g. 0x-style on EVM chains). 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter txHash: (path) Transaction hash (e.g. 0x... for EVM, or block explorer format for UTXO) 
     - parameter chain: (query) Chain the transaction belongs to (required for lookup) 
     - returns: RequestBuilder<WalletGetTransactionByHash200Response> 
     */
    open class func walletGetTransactionByHashWithRequestBuilder(txHash: String, chain: Chain_walletGetTransactionByHash) -> RequestBuilder<WalletGetTransactionByHash200Response> {
        var localVariablePath = "/api/wallet/non-custodial/transactions/{txHash}"
        let txHashPreEscape = "\(APIHelper.mapValueToPathItem(txHash))"
        let txHashPostEscape = txHashPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{txHash}", with: txHashPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "chain": (wrappedValue: chain.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletGetTransactionByHash200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get transaction history for a non-custodial address
     
     - parameter addressId: (path) Registered address ID. 
     - parameter limit: (query) Number of transactions per page. (optional, default to 50)
     - parameter page: (query) Page number (1-based). (optional, default to 1)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetTransactions(addressId: String, limit: Int? = nil, page: Int? = nil, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletGetTransactions200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetTransactionsWithRequestBuilder(addressId: addressId, limit: limit, page: page).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get transaction history for a non-custodial address
     - GET /api/wallet/non-custodial/addresses/{addressId}/transactions
     - Returns paginated transaction history for a registered non-custodial address (incoming/outgoing, status, confirmations, amounts). 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter addressId: (path) Registered address ID. 
     - parameter limit: (query) Number of transactions per page. (optional, default to 50)
     - parameter page: (query) Page number (1-based). (optional, default to 1)
     - returns: RequestBuilder<WalletGetTransactions200Response> 
     */
    open class func walletGetTransactionsWithRequestBuilder(addressId: String, limit: Int? = nil, page: Int? = nil) -> RequestBuilder<WalletGetTransactions200Response> {
        var localVariablePath = "/api/wallet/non-custodial/addresses/{addressId}/transactions"
        let addressIdPreEscape = "\(APIHelper.mapValueToPathItem(addressId))"
        let addressIdPostEscape = addressIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{addressId}", with: addressIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
            "page": (wrappedValue: page?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletGetTransactions200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get webhook delivery logs
     
     - parameter webhookId: (path) Webhook ID (MongoDB ObjectId) to get logs for. 
     - parameter limit: (query) Maximum number of log entries to return. (optional, default to 50)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletGetWebhookLogs(webhookId: String, limit: Int? = nil, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletGetWebhookLogs200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletGetWebhookLogsWithRequestBuilder(webhookId: webhookId, limit: limit).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Get webhook delivery logs
     - GET /api/wallet/non-custodial/webhooks/{webhookId}/logs
     - List delivery attempts for a wallet webhook (success/failure, payload, response, duration). Paginated by limit.
     - API Key:
       - type: apiKey X-API-Key (HEADER)
       - name: ApiKeyAuth
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter webhookId: (path) Webhook ID (MongoDB ObjectId) to get logs for. 
     - parameter limit: (query) Maximum number of log entries to return. (optional, default to 50)
     - returns: RequestBuilder<WalletGetWebhookLogs200Response> 
     */
    open class func walletGetWebhookLogsWithRequestBuilder(webhookId: String, limit: Int? = nil) -> RequestBuilder<WalletGetWebhookLogs200Response> {
        var localVariablePath = "/api/wallet/non-custodial/webhooks/{webhookId}/logs"
        let webhookIdPreEscape = "\(APIHelper.mapValueToPathItem(webhookId))"
        let webhookIdPostEscape = webhookIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{webhookId}", with: webhookIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletGetWebhookLogs200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     * enum for parameter chain
     */
    public enum Chain_walletListAddresses: String, CaseIterable {
        case ethereum = "ethereum"
        case binance = "binance"
        case bsc = "bsc"
        case polygon = "polygon"
        case arbitrum = "arbitrum"
        case optimism = "optimism"
        case base = "base"
        case avalanche = "avalanche"
        case celo = "celo"
        case bitcoin = "bitcoin"
        case litecoin = "litecoin"
        case dogecoin = "dogecoin"
        case solana = "solana"
        case tron = "tron"
        case ripple = "ripple"
        case cardano = "cardano"
        case ton = "ton"
    }

    /**
     List registered non-custodial addresses
     
     - parameter chain: (query) Filter by chain (optional) (optional)
     - parameter projectId: (query) Filter by project ID (optional). (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletListAddresses(chain: Chain_walletListAddresses? = nil, projectId: String? = nil, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletListAddresses200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletListAddressesWithRequestBuilder(chain: chain, projectId: projectId).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     List registered non-custodial addresses
     - GET /api/wallet/non-custodial/addresses
     - Returns all non-custodial addresses registered for the current project/organization. Optional filters by chain and projectId. Addresses must be registered via POST /api/wallet/non-custodial/register-address before use. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter chain: (query) Filter by chain (optional) (optional)
     - parameter projectId: (query) Filter by project ID (optional). (optional)
     - returns: RequestBuilder<WalletListAddresses200Response> 
     */
    open class func walletListAddressesWithRequestBuilder(chain: Chain_walletListAddresses? = nil, projectId: String? = nil) -> RequestBuilder<WalletListAddresses200Response> {
        let localVariablePath = "/api/wallet/non-custodial/addresses"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "chain": (wrappedValue: chain?.encodeToJSON(), isExplode: true),
            "projectId": (wrappedValue: projectId?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletListAddresses200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List wallet webhooks
     
     - parameter projectId: (query) Filter by project ID (optional). (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletListWebhooks(projectId: String? = nil, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletListWebhooks200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletListWebhooksWithRequestBuilder(projectId: projectId).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     List wallet webhooks
     - GET /api/wallet/non-custodial/webhooks
     - Returns all wallet webhooks for the current context, optionally filtered by projectId. Includes delivery stats and active status. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter projectId: (query) Filter by project ID (optional). (optional)
     - returns: RequestBuilder<WalletListWebhooks200Response> 
     */
    open class func walletListWebhooksWithRequestBuilder(projectId: String? = nil) -> RequestBuilder<WalletListWebhooks200Response> {
        let localVariablePath = "/api/wallet/non-custodial/webhooks"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "projectId": (wrappedValue: projectId?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletListWebhooks200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Register a non-custodial wallet address
     
     - parameter walletRegisterAddressRequest: (body) Public address, chain identifier, and optional derivation path and label. projectId scopes the registration to a project. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletRegisterAddress(walletRegisterAddressRequest: WalletRegisterAddressRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: NonCustodialAddressResponse?, _ error: Error?) -> Void)) -> RequestTask {
        return walletRegisterAddressWithRequestBuilder(walletRegisterAddressRequest: walletRegisterAddressRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Register a non-custodial wallet address
     - POST /api/wallet/non-custodial/register-address
     - Register a public wallet address for balance monitoring, transaction indexing, and webhook notifications. Keys are never sent to the server; generation and signing happen client-side only. Supports EVM, UTXO, Solana, Tron, TON, Cardano, and other chains. Optionally provide derivation path and label for multi-address tracking. 
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletRegisterAddressRequest: (body) Public address, chain identifier, and optional derivation path and label. projectId scopes the registration to a project. 
     - returns: RequestBuilder<NonCustodialAddressResponse> 
     */
    open class func walletRegisterAddressWithRequestBuilder(walletRegisterAddressRequest: WalletRegisterAddressRequest) -> RequestBuilder<NonCustodialAddressResponse> {
        let localVariablePath = "/api/wallet/non-custodial/register-address"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletRegisterAddressRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<NonCustodialAddressResponse>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Test a webhook delivery (sends a single test payload)
     
     - parameter walletTestWebhookRequest: (body) URL to send the test POST to; optional secret and projectId for scoping; optional event type to simulate. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletTestWebhook(walletTestWebhookRequest: WalletTestWebhookRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletTestWebhook200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletTestWebhookWithRequestBuilder(walletTestWebhookRequest: walletTestWebhookRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Test a webhook delivery (sends a single test payload)
     - POST /api/wallet/non-custodial/webhooks/test
     - Sends a test payload to the given URL to verify webhook connectivity and signature. Uses the same signing as real deliveries.
     - API Key:
       - type: apiKey X-API-Key (HEADER)
       - name: ApiKeyAuth
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter walletTestWebhookRequest: (body) URL to send the test POST to; optional secret and projectId for scoping; optional event type to simulate. 
     - returns: RequestBuilder<WalletTestWebhook200Response> 
     */
    open class func walletTestWebhookWithRequestBuilder(walletTestWebhookRequest: WalletTestWebhookRequest) -> RequestBuilder<WalletTestWebhook200Response> {
        let localVariablePath = "/api/wallet/non-custodial/webhooks/test"
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletTestWebhookRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletTestWebhook200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update a wallet webhook
     
     - parameter webhookId: (path) Webhook ID (MongoDB ObjectId) to update. 
     - parameter walletUpdateWebhookRequest: (body) Fields to update (url, events, secret, filters). Omitted fields are left unchanged. 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func walletUpdateWebhook(webhookId: String, walletUpdateWebhookRequest: WalletUpdateWebhookRequest, apiResponseQueue: DispatchQueue = MudbaseSDKAPI.apiResponseQueue, completion: @escaping ((_ data: WalletUpdateWebhook200Response?, _ error: Error?) -> Void)) -> RequestTask {
        return walletUpdateWebhookWithRequestBuilder(webhookId: webhookId, walletUpdateWebhookRequest: walletUpdateWebhookRequest).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Update a wallet webhook
     - PUT /api/wallet/non-custodial/webhooks/{webhookId}
     - Update webhook URL, events, secret, or filters. Partially applied; only provided fields are updated.
     - API Key:
       - type: apiKey X-API-Key (HEADER)
       - name: ApiKeyAuth
     - Bearer Token:
       - type: http
       - name: BearerToken
     - parameter webhookId: (path) Webhook ID (MongoDB ObjectId) to update. 
     - parameter walletUpdateWebhookRequest: (body) Fields to update (url, events, secret, filters). Omitted fields are left unchanged. 
     - returns: RequestBuilder<WalletUpdateWebhook200Response> 
     */
    open class func walletUpdateWebhookWithRequestBuilder(webhookId: String, walletUpdateWebhookRequest: WalletUpdateWebhookRequest) -> RequestBuilder<WalletUpdateWebhook200Response> {
        var localVariablePath = "/api/wallet/non-custodial/webhooks/{webhookId}"
        let webhookIdPreEscape = "\(APIHelper.mapValueToPathItem(webhookId))"
        let webhookIdPostEscape = webhookIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{webhookId}", with: webhookIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = MudbaseSDKAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: walletUpdateWebhookRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WalletUpdateWebhook200Response>.Type = MudbaseSDKAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PUT", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
