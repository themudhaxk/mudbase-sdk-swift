//
// MultiRoleUpdateCollectionPermissionsRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct MultiRoleUpdateCollectionPermissionsRequest: Codable, JSONEncodable, Hashable {

    public enum Actions: String, Codable, CaseIterable {
        case create = "create"
        case read = "read"
        case update = "update"
        case delete = "delete"
    }
    public enum DataScope: String, Codable, CaseIterable {
        case all = "all"
        case own = "own"
    }
    public var actions: [Actions]?
    public var conditions: AnyCodable?
    /** `all` = no automatic row-owner filter. `own` = only documents where the owner field (default `createdBy`) matches the authenticated app user. */
    public var dataScope: DataScope?
    /** Optional per-assignment override for the document field used when `dataScope` is `own` (project default is `settings.dataOwnerField`, usually `createdBy`). */
    public var ownerField: String?

    public init(actions: [Actions]? = nil, conditions: AnyCodable? = nil, dataScope: DataScope? = nil, ownerField: String? = nil) {
        self.actions = actions
        self.conditions = conditions
        self.dataScope = dataScope
        self.ownerField = ownerField
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case actions
        case conditions
        case dataScope
        case ownerField
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(actions, forKey: .actions)
        try container.encodeIfPresent(conditions, forKey: .conditions)
        try container.encodeIfPresent(dataScope, forKey: .dataScope)
        try container.encodeIfPresent(ownerField, forKey: .ownerField)
    }
}

