//
// AdminOrgPlanPatchRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct AdminOrgPlanPatchRequest: Codable, JSONEncodable, Hashable {

    public enum Plan: String, Codable, CaseIterable {
        case free = "free"
        case basic = "basic"
        case starter = "starter"
        case growth = "growth"
        case scale = "scale"
        case enterprise = "enterprise"
    }
    public enum TxPlan: String, Codable, CaseIterable {
        case starter = "starter"
        case growth = "growth"
        case scale = "scale"
        case enterprise = "enterprise"
    }
    public var plan: Plan
    public var reason: String?
    public var txPlan: TxPlan?

    public init(plan: Plan, reason: String? = nil, txPlan: TxPlan? = nil) {
        self.plan = plan
        self.reason = reason
        self.txPlan = txPlan
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case plan
        case reason
        case txPlan
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(plan, forKey: .plan)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(txPlan, forKey: .txPlan)
    }
}

