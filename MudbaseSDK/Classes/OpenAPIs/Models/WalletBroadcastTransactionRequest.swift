//
// WalletBroadcastTransactionRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct WalletBroadcastTransactionRequest: Codable, JSONEncodable, Hashable {

    public enum Chain: String, Codable, CaseIterable {
        case ethereum = "ethereum"
        case polygon = "polygon"
        case arbitrum = "arbitrum"
        case optimism = "optimism"
        case base = "base"
        case bsc = "bsc"
        case binance = "binance"
        case avalanche = "avalanche"
        case celo = "celo"
        case bitcoin = "bitcoin"
        case litecoin = "litecoin"
        case dogecoin = "dogecoin"
        case tron = "tron"
        case solana = "solana"
        case ton = "ton"
        case cardano = "cardano"
    }
    /** Blockchain for broadcast (EVM, UTXO, or chain-specific) */
    public var chain: Chain
    /** Fully signed transaction (hex string) */
    public var signedTx: String
    /** Address that signed the transaction (must be registered) */
    public var fromAddress: String

    public init(chain: Chain, signedTx: String, fromAddress: String) {
        self.chain = chain
        self.signedTx = signedTx
        self.fromAddress = fromAddress
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case chain
        case signedTx
        case fromAddress
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(chain, forKey: .chain)
        try container.encode(signedTx, forKey: .signedTx)
        try container.encode(fromAddress, forKey: .fromAddress)
    }
}

