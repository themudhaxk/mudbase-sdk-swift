//
// WithdrawRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct WithdrawRequest: Codable, JSONEncodable, Hashable {

    /** Recipient address (chain-specific format). */
    public var toAddress: String
    /** Amount to send (numeric; currency from wallet). */
    public var amount: Double
    /** For USDT wallets only; ETH, BSC, TRX, SOL, or POLYGON. */
    public var network: String?
    public var options: AnyCodable?

    public init(toAddress: String, amount: Double, network: String? = nil, options: AnyCodable? = nil) {
        self.toAddress = toAddress
        self.amount = amount
        self.network = network
        self.options = options
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case toAddress
        case amount
        case network
        case options
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(toAddress, forKey: .toAddress)
        try container.encode(amount, forKey: .amount)
        try container.encodeIfPresent(network, forKey: .network)
        try container.encodeIfPresent(options, forKey: .options)
    }
}

