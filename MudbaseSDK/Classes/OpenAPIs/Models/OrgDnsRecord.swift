//
// OrgDnsRecord.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct OrgDnsRecord: Codable, JSONEncodable, Hashable {

    /** DNS record type (TXT, CNAME, …) */
    public var type: String
    /** Owner name / FQDN to create at the customer's DNS host */
    public var name: String
    /** Record value or CNAME target */
    public var value: String
    /** mudbase_ownership, routing, fly_ownership, acme_challenge, or fly (legacy bucket). */
    public var purpose: String

    public init(type: String, name: String, value: String, purpose: String) {
        self.type = type
        self.name = name
        self.value = value
        self.purpose = purpose
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case name
        case value
        case purpose
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encode(name, forKey: .name)
        try container.encode(value, forKey: .value)
        try container.encode(purpose, forKey: .purpose)
    }
}

