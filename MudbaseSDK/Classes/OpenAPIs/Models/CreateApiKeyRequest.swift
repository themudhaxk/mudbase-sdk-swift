//
// CreateApiKeyRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Payload to create an API key (name, projectId, permissions, rateLimit, expiresAt). */
public struct CreateApiKeyRequest: Codable, JSONEncodable, Hashable {

    public static let nameRule = StringRule(minLength: 2, maxLength: 100, pattern: nil)
    public static let projectIdRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^[0-9a-fA-F]{24}$/")
    public var name: String
    /** MongoDB ObjectId of the project */
    public var projectId: String
    /** Optional. Permission objects (resource + actions). Omit or pass [] for full access (all resources and actions). Include only the entries you want; remove resources or actions to restrict the key. */
    public var permissions: [ApiKeyPermission]?
    public var rateLimit: RateLimit?
    /** Optional. When provided, must be a valid ISO 8601 date-time in the future. Omit for no expiration. */
    public var expiresAt: Date?

    public init(name: String, projectId: String, permissions: [ApiKeyPermission]? = nil, rateLimit: RateLimit? = nil, expiresAt: Date? = nil) {
        self.name = name
        self.projectId = projectId
        self.permissions = permissions
        self.rateLimit = rateLimit
        self.expiresAt = expiresAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case projectId
        case permissions
        case rateLimit
        case expiresAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(projectId, forKey: .projectId)
        try container.encodeIfPresent(permissions, forKey: .permissions)
        try container.encodeIfPresent(rateLimit, forKey: .rateLimit)
        try container.encodeIfPresent(expiresAt, forKey: .expiresAt)
    }
}

