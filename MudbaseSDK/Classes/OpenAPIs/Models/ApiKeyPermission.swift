//
// ApiKeyPermission.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Permission scope (resource, actions). */
public struct ApiKeyPermission: Codable, JSONEncodable, Hashable {

    public enum Resource: String, Codable, CaseIterable {
        case auth = "auth"
        case database = "database"
        case storage = "storage"
        case functions = "functions"
        case realtime = "realtime"
        case messaging = "messaging"
    }
    public enum Actions: String, Codable, CaseIterable {
        case create = "create"
        case read = "read"
        case update = "update"
        case delete = "delete"
    }
    /** Resource scope for this permission (auth, database, storage, functions, realtime, messaging) */
    public var resource: Resource
    /** Allowed actions on the resource */
    public var actions: [Actions]

    public init(resource: Resource, actions: [Actions]) {
        self.resource = resource
        self.actions = actions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case resource
        case actions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(resource, forKey: .resource)
        try container.encode(actions, forKey: .actions)
    }
}

