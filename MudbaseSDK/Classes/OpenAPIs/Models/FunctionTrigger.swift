//
// FunctionTrigger.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Function trigger config (type, event, schedule, path, method, collectionId, bucketId). */
public struct FunctionTrigger: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case http = "http"
        case event = "event"
        case document = "document"
        case file = "file"
        case webhook = "webhook"
        case wallet = "wallet"
        case cron = "cron"
        case messaging = "messaging"
    }
    public enum Method: String, Codable, CaseIterable {
        case _get = "GET"
        case post = "POST"
        case put = "PUT"
        case delete = "DELETE"
    }
    /** Trigger type */
    public var type: ModelType
    /** Event name (e.g. create, update, delete for document; uploaded, deleted for file; tx, balance for wallet) */
    public var event: String?
    /** For cron - minutely, hourly, daily, weekly, or custom cron expression */
    public var schedule: String?
    /** HTTP path for http triggers */
    public var path: String?
    public var method: Method?
    /** For document triggers - filter by collection */
    public var collectionId: String?
    /** For file triggers - filter by bucket */
    public var bucketId: String?

    public init(type: ModelType, event: String? = nil, schedule: String? = nil, path: String? = nil, method: Method? = nil, collectionId: String? = nil, bucketId: String? = nil) {
        self.type = type
        self.event = event
        self.schedule = schedule
        self.path = path
        self.method = method
        self.collectionId = collectionId
        self.bucketId = bucketId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case event
        case schedule
        case path
        case method
        case collectionId
        case bucketId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encodeIfPresent(event, forKey: .event)
        try container.encodeIfPresent(schedule, forKey: .schedule)
        try container.encodeIfPresent(path, forKey: .path)
        try container.encodeIfPresent(method, forKey: .method)
        try container.encodeIfPresent(collectionId, forKey: .collectionId)
        try container.encodeIfPresent(bucketId, forKey: .bucketId)
    }
}

