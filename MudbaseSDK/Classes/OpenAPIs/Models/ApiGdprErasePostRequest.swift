//
// ApiGdprErasePostRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ApiGdprErasePostRequest: Codable, JSONEncodable, Hashable {

    public enum Confirm: String, Codable, CaseIterable {
        case delete = "DELETE"
    }
    /** Must equal \"DELETE\" to proceed with erasure. */
    public var confirm: Confirm
    /** Required unless the account has no password set (OAuth-only) */
    public var currentPassword: String?
    /** Required only if the account has 2FA enabled */
    public var totpToken: String?

    public init(confirm: Confirm, currentPassword: String? = nil, totpToken: String? = nil) {
        self.confirm = confirm
        self.currentPassword = currentPassword
        self.totpToken = totpToken
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case confirm
        case currentPassword
        case totpToken
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(confirm, forKey: .confirm)
        try container.encodeIfPresent(currentPassword, forKey: .currentPassword)
        try container.encodeIfPresent(totpToken, forKey: .totpToken)
    }
}

