//
// EstimateNetworkFeeRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct EstimateNetworkFeeRequest: Codable, JSONEncodable, Hashable {

    public enum Currency: String, Codable, CaseIterable {
        case btc = "BTC"
        case eth = "ETH"
        case bnb = "BNB"
        case ltc = "LTC"
        case sol = "SOL"
        case trx = "TRX"
        case usdt = "USDT"
        case matic = "MATIC"
        case avax = "AVAX"
        case celo = "CELO"
        case doge = "DOGE"
        case ton = "TON"
        case ada = "ADA"
    }
    public enum Network: String, Codable, CaseIterable {
        case eth = "ETH"
        case bsc = "BSC"
        case trx = "TRX"
        case sol = "SOL"
        case polygon = "POLYGON"
    }
    public static let amountRule = NumericRule<Double>(minimum: 0, exclusiveMinimum: true, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    /** Currency code */
    public var currency: Currency
    /** Transaction amount (used for display; fee is chain-based) */
    public var amount: Double
    /** Required for USDT; network on which USDT is sent */
    public var network: Network?

    public init(currency: Currency, amount: Double, network: Network? = nil) {
        self.currency = currency
        self.amount = amount
        self.network = network
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case currency
        case amount
        case network
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(currency, forKey: .currency)
        try container.encode(amount, forKey: .amount)
        try container.encodeIfPresent(network, forKey: .network)
    }
}

