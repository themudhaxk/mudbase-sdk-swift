//
// OrgVerifyCustomDomainDnsSuccessResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct OrgVerifyCustomDomainDnsSuccessResponse: Codable, JSONEncodable, Hashable {

    public var success: Bool
    public var hostname: String
    /** Domain row status after check (typically cname_pending_staff after first TXT success from pending/failed; legacy dns_verified possible) */
    public var status: String
    public var verificationToken: String
    /** Same as dnsTxtHost (_mudbase-verify.<hostname>) */
    public var challengeHost: String
    /** Same as dnsTxtValue */
    public var expectedTxt: String
    public var dnsTxtHost: String
    public var dnsTxtValue: String
    public var edge: OrgEdgeHints?
    /** Same shape as `OrgDomainEntryWithDns.dnsRecords` when certificate provisioning ran after this successful verify; omit or empty when provisioning is disabled or not yet run. */
    public var dnsRecords: [OrgDnsRecord]?
    /** Managed certificate status after verify when provisioning is active; null otherwise */
    public var flyCertificateStatus: String?
    /** True when automated managed-certificate provisioning is configured for this deployment. */
    public var flyAcmeEnabled: Bool?
    /** When `flyAcmeEnabled` is false, why automated provisioning did not run (ops misconfiguration hint). */
    public var flyAcmeDisabledReason: String?
    /** When provisioning is enabled but certificate issuance failed, the provider error message for support; null on success. */
    public var flyProvisionError: String?
    /** When true, the legacy staff pipeline is on: status may stay `cname_pending_staff` and staff approve-cname is required even if certificate provisioning succeeds. */
    public var flyLegacyStaffPipeline: Bool?

    public init(success: Bool, hostname: String, status: String, verificationToken: String, challengeHost: String, expectedTxt: String, dnsTxtHost: String, dnsTxtValue: String, edge: OrgEdgeHints? = nil, dnsRecords: [OrgDnsRecord]? = nil, flyCertificateStatus: String? = nil, flyAcmeEnabled: Bool? = nil, flyAcmeDisabledReason: String? = nil, flyProvisionError: String? = nil, flyLegacyStaffPipeline: Bool? = nil) {
        self.success = success
        self.hostname = hostname
        self.status = status
        self.verificationToken = verificationToken
        self.challengeHost = challengeHost
        self.expectedTxt = expectedTxt
        self.dnsTxtHost = dnsTxtHost
        self.dnsTxtValue = dnsTxtValue
        self.edge = edge
        self.dnsRecords = dnsRecords
        self.flyCertificateStatus = flyCertificateStatus
        self.flyAcmeEnabled = flyAcmeEnabled
        self.flyAcmeDisabledReason = flyAcmeDisabledReason
        self.flyProvisionError = flyProvisionError
        self.flyLegacyStaffPipeline = flyLegacyStaffPipeline
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case success
        case hostname
        case status
        case verificationToken
        case challengeHost
        case expectedTxt
        case dnsTxtHost
        case dnsTxtValue
        case edge
        case dnsRecords
        case flyCertificateStatus
        case flyAcmeEnabled
        case flyAcmeDisabledReason
        case flyProvisionError
        case flyLegacyStaffPipeline
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(success, forKey: .success)
        try container.encode(hostname, forKey: .hostname)
        try container.encode(status, forKey: .status)
        try container.encode(verificationToken, forKey: .verificationToken)
        try container.encode(challengeHost, forKey: .challengeHost)
        try container.encode(expectedTxt, forKey: .expectedTxt)
        try container.encode(dnsTxtHost, forKey: .dnsTxtHost)
        try container.encode(dnsTxtValue, forKey: .dnsTxtValue)
        try container.encodeIfPresent(edge, forKey: .edge)
        try container.encodeIfPresent(dnsRecords, forKey: .dnsRecords)
        try container.encodeIfPresent(flyCertificateStatus, forKey: .flyCertificateStatus)
        try container.encodeIfPresent(flyAcmeEnabled, forKey: .flyAcmeEnabled)
        try container.encodeIfPresent(flyAcmeDisabledReason, forKey: .flyAcmeDisabledReason)
        try container.encodeIfPresent(flyProvisionError, forKey: .flyProvisionError)
        try container.encodeIfPresent(flyLegacyStaffPipeline, forKey: .flyLegacyStaffPipeline)
    }
}

