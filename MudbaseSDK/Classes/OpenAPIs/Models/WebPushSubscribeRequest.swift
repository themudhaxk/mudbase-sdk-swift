//
// WebPushSubscribeRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct WebPushSubscribeRequest: Codable, JSONEncodable, Hashable {

    public var subscription: WebPushSubscription
    /** Optional end-user id to associate with this subscription, for targeted sends. */
    public var userId: String?
    /** Optional client-supplied device identifier. */
    public var deviceId: String?

    public init(subscription: WebPushSubscription, userId: String? = nil, deviceId: String? = nil) {
        self.subscription = subscription
        self.userId = userId
        self.deviceId = deviceId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case subscription
        case userId
        case deviceId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(subscription, forKey: .subscription)
        try container.encodeIfPresent(userId, forKey: .userId)
        try container.encodeIfPresent(deviceId, forKey: .deviceId)
    }
}

