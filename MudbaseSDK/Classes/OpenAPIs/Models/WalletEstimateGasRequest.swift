//
// WalletEstimateGasRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct WalletEstimateGasRequest: Codable, JSONEncodable, Hashable {

    public enum Chain: String, Codable, CaseIterable {
        case ethereum = "ethereum"
        case binance = "binance"
        case bsc = "bsc"
        case polygon = "polygon"
        case arbitrum = "arbitrum"
        case optimism = "optimism"
        case base = "base"
        case avalanche = "avalanche"
        case celo = "celo"
        case bitcoin = "bitcoin"
        case litecoin = "litecoin"
        case dogecoin = "dogecoin"
        case solana = "solana"
        case tron = "tron"
        case ton = "ton"
        case cardano = "cardano"
    }
    /** Chain id. For EVM, transaction is required. For non-EVM (UTXO, Solana, Tron, TON, Cardano) only chain is needed. */
    public var chain: Chain
    public var transaction: WalletEstimateGasRequestTransaction?

    public init(chain: Chain, transaction: WalletEstimateGasRequestTransaction? = nil) {
        self.chain = chain
        self.transaction = transaction
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case chain
        case transaction
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(chain, forKey: .chain)
        try container.encodeIfPresent(transaction, forKey: .transaction)
    }
}

