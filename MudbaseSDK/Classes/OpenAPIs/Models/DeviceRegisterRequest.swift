//
// DeviceRegisterRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DeviceRegisterRequest: Codable, JSONEncodable, Hashable {

    public enum Platform: String, Codable, CaseIterable {
        case ios = "ios"
        case android = "android"
        case web = "web"
        case unknown = "unknown"
    }
    /** The device push token issued to your app by its push client. */
    public var token: String
    /** The device platform. Defaults to `unknown` when omitted or unrecognized. */
    public var platform: Platform? = .unknown

    public init(token: String, platform: Platform? = .unknown) {
        self.token = token
        self.platform = platform
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case token
        case platform
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(token, forKey: .token)
        try container.encodeIfPresent(platform, forKey: .platform)
    }
}

