//
// CreateCollectionRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Payload to create a collection (name, slug, fields, permissions, settings). */
public struct CreateCollectionRequest: Codable, JSONEncodable, Hashable {

    public var name: String
    public var slug: String?
    public var fields: [Field]
    public var permissions: [Permission]?
    public var settings: AnyCodable?

    public init(name: String, slug: String? = nil, fields: [Field], permissions: [Permission]? = nil, settings: AnyCodable? = nil) {
        self.name = name
        self.slug = slug
        self.fields = fields
        self.permissions = permissions
        self.settings = settings
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case slug
        case fields
        case permissions
        case settings
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(slug, forKey: .slug)
        try container.encode(fields, forKey: .fields)
        try container.encodeIfPresent(permissions, forKey: .permissions)
        try container.encodeIfPresent(settings, forKey: .settings)
    }
}

