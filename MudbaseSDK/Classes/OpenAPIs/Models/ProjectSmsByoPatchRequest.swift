//
// ProjectSmsByoPatchRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ProjectSmsByoPatchRequest: Codable, JSONEncodable, Hashable {

    public enum Provider: String, Codable, CaseIterable {
        case twilio = "twilio"
        case termii = "termii"
        case africastalking = "africastalking"
        case _none = "none"
    }
    public var enabled: Bool?
    public var provider: Provider?
    /** Default sender (E.164 for Twilio; Termii/Africa's Talking may use alphanumeric or approved sender IDs per provider rules). */
    public var defaultFrom: String?
    /** Provider credentials and options (encrypted at rest). Required keys when enabling BYO: **twilio** — `accountSid`, `authToken`. Optional `from`. **termii** — `apiKey`. Optional `from`. **africastalking** — `username`, `apiKey`. Optional `from`.  */
    public var config: [String: AnyCodable]?

    public init(enabled: Bool? = nil, provider: Provider? = nil, defaultFrom: String? = nil, config: [String: AnyCodable]? = nil) {
        self.enabled = enabled
        self.provider = provider
        self.defaultFrom = defaultFrom
        self.config = config
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case enabled
        case provider
        case defaultFrom
        case config
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(enabled, forKey: .enabled)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encodeIfPresent(defaultFrom, forKey: .defaultFrom)
        try container.encodeIfPresent(config, forKey: .config)
    }
}

